
[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
evm_version = "cancun"

# Strict optimization for production
optimizer = true
optimizer_runs = 200

# Remappings to handle OpenZeppelin imports cleanly
remappings = ['@openzeppelin/=lib/openzeppelin-contracts/']
